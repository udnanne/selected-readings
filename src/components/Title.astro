---
interface Props {
    classname: string;
    title: string[];
}

const { classname, title } = Astro.props;
---

<h2 class={`title title--${classname}`}>
    {
        title.map((item) => {
            return <span>{item}</span>;
        })
    }
</h2>
